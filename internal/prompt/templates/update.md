## UPDATE MODE — Modify Existing Code

This resource **already has code** — either a committed implementation or
your own previous attempt. The files below are that code. Do **not** rewrite
them from scratch.

Make the **minimal change** required by the "CHANGES TO MAKE" and/or
"## Previous Errors" sections. Preserve everything else — structure, naming,
formatting, unrelated logic. A minor bug gets a minor fix.
Output the full content of every file you change (using the `// path:` annotation),
and only those files. Unchanged files do not need to be re-emitted.
